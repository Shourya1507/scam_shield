{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKER",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m cli.scamshield_cli serve-webhook --type email",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
